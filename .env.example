# Environment Variables - NEVER COMMIT .env.local to GitHub!
# Copy this file to .env.local and fill in your actual values locally

# =====================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =====================================================
# Get these from: Supabase Dashboard > Project Settings > API
# Note: These are PUBLIC keys (anon key), safe for client-side use
# Security is enforced via Row Level Security (RLS) policies
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =====================================================
# APPLICATION SETTINGS
# =====================================================
VITE_APP_ENV=production

# =====================================================
# FUTURE USE - GEMINI API
# =====================================================
# Only needed when Gemini API is integrated for AI features
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# =====================================================
# OPENAI API (for AI Question Generator)
# =====================================================
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# =====================================================
# IMPORTANT SECURITY NOTES
# =====================================================
# 1. All VITE_ prefixed variables are exposed to client-side JavaScript
#    This is OK for Supabase anon key - it's designed to be public
# 2. Security is enforced by Row Level Security (RLS) on Supabase:
#    - Public can READ mentor data (for website)
#    - Only authenticated users can INSERT/UPDATE/DELETE (admin)
# 3. Use Vercel dashboard to set environment variables for production
# 4. Local development: Use .env.local (already in .gitignore)
# 5. Never commit .env.local or any file with real secrets
# 5. Keep this .env.example file updated for team reference
