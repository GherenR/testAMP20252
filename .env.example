# Environment Variables - NEVER COMMIT .env.local to GitHub!
# Copy this file to .env.local and fill in your actual values locally

# API Keys - SENSITIVE DATA
# Add to Vercel dashboard under Project Settings > Environment Variables

# FUTURE USE: Only needed when Gemini API is integrated
# (AI-powered mentor recommendations, smart matching, profile analysis)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Application Environment
VITE_APP_ENV=production

# IMPORTANT SECURITY NOTES:
# 1. All VITE_ prefixed variables are exposed to client-side JavaScript
#    DO NOT store sensitive secrets here
# 2. Use Vercel dashboard to set environment variables for production
# 3. Local development: Use .env.local (already in .gitignore)
# 4. Never commit .env.local or any file with real secrets
# 5. Keep this .env.example file updated for team reference
